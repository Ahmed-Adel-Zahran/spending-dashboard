# Optional: Anthropic API key for AI-powered transaction categorization.
# Without it, the app falls back to rule-based categorization (works great for most merchants).
VITE_ANTHROPIC_API_KEY=your-api-key-here
